#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * Main - program that prints the alphabet in lowercase
 *
 * Description: Using mainfuction
 *
 * Returns: Always 0.
 */

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	putchar(c);

	putchar('\n');

	return (0);
}
